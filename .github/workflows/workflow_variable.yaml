name: Testing workflow variable

on:
  workflow_dispatch:

env:
  Car: beat

jobs:
  car_name:
    runs-on: ubuntu-latest
    env:
      Owner: barath

    steps:
      - name: "car ownership"
        run: echo "$Owner owns a  $Car"
